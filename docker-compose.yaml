services:
  postgres:
    image: postgres:15
    container_name: inbox-mind-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: inbox_mind
    ports:
      - '5432:5432'
