# Mock API (use 'true' for development with mock data, 'false' for real backend)
VITE_USE_MOCK=false

# Google OAuth Client ID (not actually needed for redirect flow, but kept for compatibility)
# The backend handles Google OAuth directly
# Get this from https://console.cloud.google.com/
VITE_GOOGLE_CLIENT_ID=your-google-client-id-here.apps.googleusercontent.com

# API Base URL (for production deployment)
# VITE_API_BASE_URL=https://api.yourdomain.com
